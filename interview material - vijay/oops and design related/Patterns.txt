
1) What is design patterns?
   - Design patterns are pre-made plans for building software
   - They give developers ideas to how to organize and structure their code to make it easier
     to understand, change & reuse.
   - Types of patterns: 
     - Creational Patterns:
       - Creation patterns is group of design patterns that focused on the process of creating objects.
       - They help you to create objects in more organized, flexible and controlled way.
       - Singleton, Factory pattern

   - Behavioral Patterns:
     - These paaterns address how objects interact and communicate with each other to achieve specific behaviour
     - Chain of responsibility, Observer pattern

2) Singleton Pattern: Link: http://csharpindepth.com/Articles/General/Singleton.aspx
   - The singleton pattern is creational pattern
   - Essentially, a singleton is a class which only allows a single instance of itself to be created, and usually gives simple access to that instance.
   - Most commonly, singletons don't allow any parameters to be specified when creating the instance - 
     as otherwise a second request for an instance but with a different parameter could be      problematic! 
   - This real-world code demonstrates the Singleton pattern as a LoadBalancing object. Only a single instance (the singleton) of the class can be created 
     because servers may dynamically come on-or off-line and every request must go throught the one object that has knowledge 
     about the state of the (web) farm.
  Example: 
  class LoadBalancer
  {
    private static LoadBalancer _instance;
    private List<string> _servers = new List<string>();
    private Random _random = new Random();
 
    // Lock synchronization object
    private static object syncLock = new object();
 
    // Constructor (protected)
    protected LoadBalancer()
    {
      // List of available servers
      _servers.Add("ServerI");
      _servers.Add("ServerII");
      _servers.Add("ServerIII");
      _servers.Add("ServerIV");
      _servers.Add("ServerV");
    }
 
    public static LoadBalancer GetLoadBalancer()
    {
      // Support multithreaded applications through
      // 'Double checked locking' pattern which (once
      // the instance exists) avoids locking each
      // time the method is invoked
      if (_instance == null)
      {
        lock (syncLock)
        {
          if (_instance == null)
          {
            _instance = new LoadBalancer();
          }
        }
      }
 
      return _instance;
    }
 
    // Simple, but effective random load balancer
    public string Server
    {
      get
      {
        int r = _random.Next(_servers.Count);
        return _servers[r].ToString();
      }
    }
  }

 Example : attempted thread-safety using double-check locking
  public sealed class Singleton
  {
    private static Singleton instance = null;
    private static readonly object padlock = new object();

    Singleton()
    {
    }

    public static Singleton Instance
    {
        get
        {
            if (instance == null)
            {
                lock (padlock)
                {
                    if (instance == null)
                    {
                        instance = new Singleton();
                    }
                }
            }
            return instance;
        }
    }
  }

 - Example 2:
    Suppose we want to create logging system for your application and you want to ensure there is only one instance of the 
    logger accross your entire application.
    public class Logger
    {
       private static Logger _instance;
       private static readonly object _lock = new object();

       private Logger(){};
       
       public static Logger Instance 
       {
	 get 
	 {
	   if(_instance == null)
	   {
		lock(_lock)
		{
		   if(_instance == null)
		   {
			_instance = new Logger();
		   }
		}
	   }
	   return _instance;
	 }
       }
    }

3) Factory Patterns: 
   1) Factory Design Pattern falls under the category of creational design pattern.
   2) It deals with the problem of creating objects without specifying the exact class of object that will be created. 
   3) Define an interface for creating an object, but let the classes that implement the interface decide which class to instantiate. 
      The Factory method lets a class defer instantiation to sub classes.
   4) Here we define an interface which will create families of related or dependent objects.
   5) In simple words, interface will expose multiple methods each of which will create some object.
   6) here method return types will be generic interfaces. All these objects will together become part of some important functionality.
   Links:
   https://www.c-sharpcorner.com/UploadFile/questpond/design-pattern-interview-questions/
      
   

	AbstractFactory- IMobilePhone
	ConcreteFactory - Nokia, Samsung
	AbstractProduct- ISmartPhone, INormalPhone
	Product- NokiaPixel, Nokia1600, SamsungGalaxy, SamsungGuru
	Client- MobileClient
   
	public interface IMobilePhone
    {
        ISmartPhone CreateSmartPhone();
        INormalPhone CreateNormalPhone();
    }

    public class Nokia : IMobilePhone
    {

        public ISmartPhone CreateSmartPhone()
        {
            return new NokiaLumia();
        }

        public INormalPhone CreateNormalPhone()
        {
            return new Nokia1000();
        }
    }


    public class Samsung : IMobilePhone
    {

        public ISmartPhone CreateSmartPhone()
        {
            return new SamsungGalaxy();
        }

        public INormalPhone CreateNormalPhone()
        {
            return new SamsungGuru();
        }
    }
    public interface ISmartPhone
    {
        public string GetModelDetails();
        
    }

    public interface INormalPhone
    {
        public string GetModelDetails();
    }

    public class Nokia1000 : INormalPhone
    {
        public string GetModelDetails()
        {
            return "Normal Phone Details";
        }
    }

    public class NokiaLumia : ISmartPhone
    {
        public string GetModelDetails()
        {
            return "Smart Phone Details";
        }
    }

    public class SamsungGuru : INormalPhone
    {
        public string GetModelDetails()
        {
            return "Normal Phone Details";
        }
    }

    public class SamsungGalaxy : ISmartPhone
    {
        public string GetModelDetails()
        {
            return "Smart Phone Details";
        }
    }

    public class client
    {
        public static void main()
        {
            IMobilePhone nokia = new Nokia();
            IMobilePhone samsung = new Samsung();

            nokia.CreateNormalPhone();
            nokia.CreateSmartPhone();

            samsung.CreateNormalPhone();
            samsung.CreateSmartPhone();
        }
    }


4) Facade Pattern
   1) if the software system is composed of number of subsystems (which generally is the case) 
      we may want to create a  facade  object which would call all these subsystems from one place.
   2) It provides a single simplified interface and hence makes the subsystems easier to use.
   3) Facade object decouples the subsystem from the client and other subsystems.
   4) The Facade design pattern is particularly used when a system is very complex or difficult to understand 
      because system has a large number of interdependent classes or its source code is unavailable.

    class SubsystemA
    {
     public string OperationA1()
     {
      return "Subsystem A, Method A1\n";
     }
     public string OperationA2()
     {
      return "Subsystem A, Method A2\n";
     }
   }
   class SubsystemB
   {
    public string OperationB1()
    {
     return "Subsystem B, Method B1\n";
    }
 
    public string OperationB2()
    {
     return "Subsystem B, Method B2\n";
    }
   } 
   class SubsystemC
   {
    public string OperationC1()
    {
     return "Subsystem C, Method C1\n";
    }
 
    public string OperationC2()
    {
     return "Subsystem C, Method C2\n";
    }
  }
 
  public class Facade
  {
   SubsystemA a = new SubsystemA();
   SubsystemB b = new SubsystemB();
   SubsystemC c = new SubsystemC();
   public void Operation1()
   {
    Console.WriteLine("Operation 1\n" +
    a.OperationA1() +
    a.OperationA2() +
    b.OperationB1());
  }
  public void Operation2()
  {
   Console.WriteLine("Operation 2\n" +
   b.OperationB2() +
   c.OperationC1() +
   c.OperationC2());
  }
}

5) CQRS Pattern in microservices
  - CQRS stands for Command Query Responsibility Segregation
  - It basically sperates responsibility of read and write data operation
  - It offers Speration Of Concerns principle and seprate reading database & writing database.
  - In this we can create No-SQL database for read & RDBMS database for CRUD operations

6) How to sync databases using CQRS?
  - This can be solve using Event Driven Architecture using message broker.
  - Here we can use Kafka topic exchange using publish/subscribe pattern

7) Saga Pattern in microservices:
  - Saga pattern is used to maintain data consistency accross the microservices
  - This mechanism allows us to rollback all completed transactions in case of any microservice failed.
  - To roll back we used compensating transactions which will be responsible for reverting completed transactions operations 
  - We can use Orchastrator Saga type with the help of azure durable function. This will single point to handle this.  


8) Repository Patterns:   
  - Create an abstraction layer between the data access layer and the business logic layer of an application
  - So basically it creates layer between your business and databse layer, so in future we have decided to change our database from sql to oracle
    it will wont affect your business layer
  - In repository pattern we will create one interface for one repository class
    Interface only contains defination for crud operations and repository class implement this interface
  - Generic repository:
    - Creating a repository class for each entity type could result in a lot of redundant code, and it could result in partial updates.
    - For example, suppose you have to update two different entity types as part of the same transaction. 
      If each uses a separate database context instance, one might succeed and the other might fail. 
    - So minimize the code redundancy we use generic repository and one way to ensure all entities will use same db context 
      we use unit of work
    - Unit of work ensures that when you use multiple repositories, they share single database context only.

9) Chain of responsibility pattern:
   - The Chain of Responsibility pattern is a behavioral design pattern that allows an object to pass 
     a request along a chain of potential handlers until the request is handled or reaches the end of the chain.
   - Example : Leave Approval.
     Public class LeaveRequest
     {
	public string Employee{ get;set; };
        public int Days{ get;set; };
     }

     //handler
     Public abstract class LeaveHandler
     {
	protected LeaveHandler _nextHandler;
        public void SetNextHandler(LeaveHandler nextHandler)
        {
	   nextHandler = nextHandler;
        }
	
	public abstract void HandleRequest(LeaveRequest leaveRequest);
     }

     Public class TeamLeader : LeaveHandler
     {
	public override void HandleRequest(LeaveRequest leaveRequest)
	{
	   if(leaveRequest.Days <= 2)
	   {
	      console.writeline("Leave Approved by team leader");
	   }
           else if(nextHandler != null)
	   {
	      _nextHandler.HandleRquest(leaveRequest);
	   }
	}
     }

     public class Manager : LeaveHandler
     {
	public override HandleRequest(LeaveRequest leaveRequest)
	{
	   if(leaveRequest<=5)
	   {
	     console.writeline("Leave Approved by manager.")
	   }
           else if(nextHandler!=null)
	   {
	     _nextHandler.HandleRequest(leaveRequest);
	   }
	}
     }

     public class Director : LeaveHandler
     {
	public override HandleRequest(LeaveRequest leaveRequest)
	{
	   if(leaveRequest >= 5)
	   {
	     console.writeline("Leave Approved by director.")
	   }
           else 
	   {
	     console.writeline("Leave Not Approved by director.")
	   }
	}
     }

     class Program
     {
	public static void main()
	{
	  LeaveHandler teamLeader = new TeamLeader();
	  LeaveHandler manager = new Manager();
	  LeaveHandler director = new Director();
          
	  teamLeader.SetNextHandler(manager);
          manager.SetNextHandler(director);
	  LeaveRequest request1 = new LeaveRequest();
          request1.Days = 5;
	  request1.Employee = "Shri";	  
          teamLeader.HandleRequest(request1);
	}
     }

8) Observer pattern with example in c#
   - It defines one to many dependency between objects.
   - When one object (the subject) changes its state, all the dependent ojects (Observers) are notified and updated automatically.
   - Example:
     - NewsPublisher is subject, EmailSubcriber & SMSSubscriber are observer to NewsPublisher.
     - So whenever any news is published by NewsPublisher 

9) Dependancy Injection 
  - Dependency Injection (DI) is a software design pattern that allow us to develop loosely coupled code.
  - DI is a great way to reduce tight coupling between software components.
  - DI also enables us to better manage future changes and other complexity in our software.
  - The process of injecting (converting) coupled (dependent) objects into decoupled (independent) objects is called Dependency Injection.
  - Diffrent Ways to implement 
    - Constructor Injection
      Dependency Injection is done by supplying the DEPENDENCY through the class s constructor when instantiating that class.
      public class IService
      {
	void serve();
      }
      
      Public class service: Iservice
      {
         public void serve()
	 {
	   console.log("serve");
	 }
      }
      
      public class client
      {
         private Iservice _service;
         public client(Iservice service)
         {
            this._service=service;
         }
         
         public void start()
         {
           console.log(_service.serve());
         }
            
      }
      
      public class program
      {
      	  Client client=new Client(new Service());
  	  cleient.start();          
      }
     
    - Property injection
      Property Injection is impelemented by using class properties.
      public interface Iservice
      {
 	void serve();
      }
      
      public class service:Iservice
      {
 	public void serve()
	{
	  console.log("serve");
	}
      }

      public class client 
      {
	private Iservice _service;
	public Iservice service
        {
	  set {this._service=value}
	}
        
 	public vois start()
	{
	  console.log(_service.serve());
	}
      }
      
      public class program
      {
	Client client=new Client();
	client.service=new Service();
	client.start();	
      }
    - Method Injecton 
      Dependancy injection is implemented by passing dependant objects to method
      public interface Iservice
      {
	void serve();
      }
      
      public class Service: Iservice
      {
	public void serve()
	{
	 console.log("serve");	
	}
      }

      public class client
      {
	Iservice _service;
	public void start(Iservice service)
	{
	 this._service=service;
	 service.start();	
	}
      }
      
     public class program
     {
	Client client=new Client();
	client.start(new Service());
     }

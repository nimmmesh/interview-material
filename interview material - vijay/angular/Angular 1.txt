	Interview Questions Lonk: 
- https://www.fullstack.cafe/blog/angular-7-interview-questions-to-crack-in-2019

1) How to improve angular application?
   - Lazy Loading Modules
   - AOT Compilation
   - Use OnePush Change Detection Stetaregy.
   - Implement Caching
   - Use Pure/Aync Pipes
   - Implement server side pagination and infinite scrolling
   - Optimize API Calls by combining multiple requests or use techniques like caching and pagination
   - Use bundling & Minifications

2) What is an AOT compilation? What are its advantages?
   - The Ahead-of-time (AOT) compiler converts the Angular HTML and TypeScript code into JavaScript code during the build phase,
     i.e., before the browser downloads and runs the code.
   - Advantages:
     1. Faster rendering
     2. Fewer asynchronous requests
     3. Smaller Angular framework download size
3) Aync Pipe?
   - It basically used with Observables & Promises. 
   - It automatically subscribes to observable and retrieves data & update the view whenever new data is available.

4) What is pure Pipe?
   - It uses pure functions, so a pure pipe dont use any internal state, pure pipe re-evaluate only when input value change.
   - If input value remains same, the pipe's result cache and reused.
   - A single instance of pure pipe is used throughout the application

5) What is impure?
   - In this for every change detection cycle in angular, impure pipe is called regardless of the change in 
     the input fields
   - Multiple pipe insances are created for those pipes
     @Pipe({
  	    name: 'demopipe',
 	    pure : true/false 
	})
6) Life cycle hooks in angular : 
    - Directive and component instances have a lifecycle as Angular creates, updates, and destroys them. 
    - Developers can tap into key moments in that lifecycle by implementing one or more of the lifecycle hook interfaces in the Angular core library.

    - ngOnChanges - Executes every time the value of input property changes, The hook method recieives a simple changes object containing
		    current and previous property values. This is called before ngOnInit.	

    - ngOnInit - Executes after the constructor and after ngOnChange hook for the first time. 
		 it is commonly used for component initialisation and retriveing data from a database.

    - ngDoCheck - This is for the detection and to act on changes that Angular can't or won't detect on its own.

    - ngAfterContentInit - This is called in response after Angular projects external content into the component's view.

    - ngAfterContentChecked - This is called in response after Angular checks the content projected into the component.

    - ngAfterViewInit - This is called in response after Angular initializes the component's views and child views.

    - ngAfterViewChecked - This is called in response after Angular checks the component's views and child views.

    - ngOnDestroy - This is the cleanup phase just before Angular destroys the directive/component.

4) What are Route Guards? 
    - Angular s route guards are interfaces which can tell the router whether or not it should allow navigation to a requested route.
    - They make this decision by looking for a true or false return value from a class which implements the given guard interface.
    - https://medium.com/@ryanchenkie_40935/angular-authentication-using-route-guards-bf7a4ca13ae3 => JWT Authentication
    - https://codecraft.tv/courses/angular/routing/router-guards/
    - There are five different types of guards and each of them is called in a particular sequence.
      1) CanActivate
	 Checks to see if a user can visit a route.

      2) CanActivateChild
	 Checks to see if a user can visit a routes children.

      3) CanDeactivate
	 - Checks to see if a user can exit a route.
         - Used to warn people if they are navigating away from a page where they have some unsaved changes.

      4) Resolve
 	 - Performs route data retrieval before route activation.
         - ensure whether there is data available or not before navigating to any route. 

      5) CanLoad
	 Checks to see if a user can visit to a module that lazy loaded.

5) What Is RxJS?
   - RxJS(Reactive extensions for JavaScript) is a library for handling asynchronous and event based programming 
     using observables

6) What is observables?
   - An observable is data stream that emits values over time. it emits values, error & completion signal.

7) What Are Observers?
   - Observers are the objects which subscribe to observables to receieve emitted values, erros & completion signal

8) Types of subject?
   - Behavioural Subject:
     - It always returns the current value to its subscribers even if the subscribe after the value has been emitted
   
   - Replay Subject: 
     - Records multiple values from observable and replays or returns them to observers
     - in this we have to specify the buffer size for how many value you want emmit.

   - AsyncSubject:
     - Only emits last value of observable execution
     - it will only emit a value when the execution completes.

     
9) What Are Operators in RxJS?
   - Operators are function which transform, filter and combine observables to create new observables
   - map, filter, merge, concat

10) map operator?
   - it is basically use to data transformation such as formating data, extracting specific values or apply calculation logic

11) Filter Operator?
   - this is useful for filtering data on specific creteria.

12) Pipe operator?
   - This is useful to composing operators and building chain of operations on observables
   - Example
     import { of } from 'rxjs';
     import { map, filter, catchError } from 'rxjs/operators';
     const source = of(1, 2, 3, 4, 5);

     const transformedObservable = source.pipe(
      map(value => value * 2),
      filter(value => value > 5),
      catchError(error => of(`Error: ${error}`))
     );

   transformedObservable.subscribe(result => console.log(result));


13) debounceTime?
   - It basically delays rate at which observable emits the value
   - Basically use when we deal with user inputs related operations and you want avoid 
     frequent api calls on every input change

14) What is ngModule?
   - In Angular, ngModule is core building block that helps you organize and manage components, 
     directives, Services & other pieces of your application.
   - declarations: This array includes the components that are part of this module.
   - imports: This array specifies the imported modules. for example: HTTPModule, BrowserModule
   - Bootstrap : This array contains the root component(s) that should be loaded when the application starts. 
                 In this case, AppComponent is the root component.
   - Providers : it is used to configure dependency injection, it allows us to specify services which 
                 we can inject in component, directives or other services throughout the entire module


  
15) Steps to create Component?
   - Import component from angular core
   - Create typescript class with export keyword
   - Add @component decorator to class
   - Add selector, templateurl, styleurls in decorator.

16) What is the difference between AOT and JIT? 
  - AOT : 
    - This will compile html & javascript code during build phase before browser downloads & run the code. 
    - This ensure faster rendering to the browser.
    - To specify AOT compilation, include the --aot option with the ng build or ng serve command. 
  - JIT : 
    - This will compile computer code to machine code during execution or runtime.
    - it is also known as dynamic compilation
    - JIT Compilation is default when you run the ng build or ng serve.

17) Explain the @Component Decorator. 
   - it is basically use for creating components
   - this will allows us to specify the metdata which containd the information related to component
   - this will contains selector, template, templateurl, styleurls

18) What are Services in Angular?
   - Services is the place where we write our common logic where this can be used by multiple app components
   - A service can be written once and injected into all the required components.

19) How to make service injactable?
   - you need to decorate the service class with the @Injectable()
  
20) Different ways to inject services?
   - Constructor Injection
   - Property Injection
   - Static Method Injection : 
     import { Component, Injector } from '@angular/core';
     const myService = this.injector.get(MyService);

21) What are Promises and Observables in Angular? 
    Promises:    
    - As soon as promises made or created it gets executed
    - Promises emits single value at a time 
    - They are Push errors to the child promises. 
    
   Observables : 
    - Observables are bit lazy, nothing happens until subscription is made
    - Observables are like stream, as it emits multiple values
  
12) What is ngOnInit?
    - It is lifecycle hook and it is executed once the component is created
    
13) How to use ngFor in a tag? 
    <ul> <li *ngFor="let order in orderlist"> {{item.orderId}} </li> </ul?

14) What are Template-Driven Forms in Angular?
    - It provides way to handle form submission using Directives and template syntax.
    - They are suitable for simpler forms that require less custom validation & logic compared to reactive forms
    - most of the interaction occurs in the template
    - it uses formsModule inside imports
    <form #myForm="ngForm" (ngSubmit)="onSubmit(myForm)">
      <label for="name">Name:</label>
      <input type="text" id="name" name="name" ngModel required>
  
      <label for="email">Email:</label>
      <input type="email" id="email" name="email" ngModel required email>  
      <button type="submit" [disabled]="!myForm.valid">Submit</button>
    </form>
    
15) What are the Key Directives Used in Template-Driven Forms?
    - ngForm
    - ngModel
    - ngSubmit


16) How Do You Add Validation to Template-Driven Forms?
   - Validation can be added using attributes such as required, min, max, pattern
   - Angular applies CSS classes like ng-valid, ng-invalid, ng-touched, etc., to style and indicate validation status.

17) How Do You Access Form Control Values in Template-Driven Forms?
   - By using "value" property

18) How Do You Disable the Submit Button Until the Form is Valid?
   - By binding the [disabled] attribute of the submit button
   - [disabled]="!myForm.valid".

19) Limitations of Template Driven Forms?
   - Limited Customization for validations & complex forms interaction
   - Less Testable
   - Limited Scalability : Becomes harder to handle/manage complex forms

20) Reactive Forms?
   - reactive-driven forms, most of the interaction occurs in the component.
   - It provides more control & customization compared to template driven forms
   - it uses ReactiveFormsModule inside imports

21) What Are the Key Components of Reactive Forms?
   - FormGroup :
     Represents a group of form controls.
   - FormControl : 
     Represents an individual form control with its value and validation state.
   - FormBuilder 
     A service which simplifies the creation of formgroup & formcontrol's instance

22) How Do You Create a FormGroup and FormControl?
    - Use FormBuilder Service to create a FormGroup & define formcontrol within it
    - Example : 
      import { FormBuilder, FormGroup, Validators } from '@angular/forms';
      myForm: FormGroup;
      constructor(private fb: FormBuilder) {
        this.myForm = this.fb.group({
        name: ['', Validators.required],
        email: ['', [Validators.required, Validators.email]],
       });
      }
     
23) How Do You Handle Form Validation in Reactive Forms?
   - Inside Form Control Instance we can specify validators like required, email or you can create custom validators
   - Custom Validation Creation steps
     - First create custom validation function in any ts file using AbstractControl, ValidationErrors from angular forms package
     - Then import that function component, & add that in validators in formcontrol instance.
   
24) How Do You Bind Form Controls to Template Elements?
   - By using [formControl] directives

25) How Can You Dynamically Add or Remove Form Controls?
   - You can use methods like addControl, removeControl, addFormGroup, and removeFormGroup on the FormGroup instance

26) How Do You Handle Form Submission?
    - You can handle form submission by binding a method to the (ngSubmit) event of the form element. 
     <form [formGroup]="myForm" (ngSubmit)="onSubmit()">
     <!-- Form controls go here -->
     <button type="submit" [disabled]="!myForm.valid">Submit</button>
     </form>
   
27) setValue vs patchValue?
   - setValue
     - When you need to set all form control values and enforce the form group's validity to reflect the entire state of the form.
     - In this we have to set all formcontrols values
   - patchValue
     - This is also used to set the value of formcontrol but it does not update its validity
     - In this we can change the value of one or more form controls

28) What Are the Advantages of Reactive Forms Over Template-Driven Forms?
   - Greater Flexibility
   - Complex Validation
   - Improved testability

29) Eager Loading Stratergy:
    - Eager loading is the default loading strategy in Angular
    - Eager loading loads all the necessary components and modules at application startup
    - This will useful for smaller application as it will not impact initial load of application

30) Lazy Loading Startergy:
    - In Lazy loading we load modules & components which requires for application start. & load other components and modules
      on demand depending on router state
   
31) How we can achieve lazy loading?
    - By using loadChildren property in route configuration.    

32) How to achieve dynamic lazy loading?
   - First create service which fetches all the routes depending on your user roles.

     import { Injectable } from '@angular/core';
     import { HttpClient } from '@angular/common/http';
     import { Observable } from 'rxjs';

     @Injectable()
     export class DynamicRouteService 
     {
       constructor(private http: HttpClient) {}
       getDynamicRoutes(): Observable<any> {
         return this.http.get<any>('api/routes');
       }
     }
   - Then create one component which which will use that service to get dynamic routes and generates 
     the routes array
     import { Component, OnInit } from '@angular/core';
     import { Router, ActivatedRoute, Routes } from '@angular/router';
     import { DynamicRouteService } from './dynamic-route.service';

     @Component({
     selector: 'app-route-loader',
     template: '<router-outlet></router-outlet>',
     })
     export class RouteLoaderComponent implements OnInit {
       constructor(private route: ActivatedRoute, private router: Router, private dynamicRouteService: DynamicRouteService) {}

      ngOnInit() {
        this.dynamicRouteService.getDynamicRoutes().subscribe(routesData => {
        const routes: Routes = [];
        routesData.forEach(route => {
         routes.push({
           path: route.path,
           loadChildren: () => import(route.modulePath).then(m => m[route.moduleName]),
         });
       });

       this.router.config.unshift(...routes);
     });
     }
    }
  
   - Configure that Component in app routing module
     const routes: Routes = [
  	{ path: '', pathMatch: 'full', redirectTo: 'route-loader' },
  	{ path: 'route-loader', component: RouteLoaderComponent },
     ];

    @NgModule({
    imports: [RouterModule.forRoot(routes)],
    exports: [RouterModule],
    })
    export class AppRoutingModule {}
         
33) What if i dont sepcify provideIn root in injactable service?
   - in this case we dont have to add services in providers array of module or components.

42) What exactly is the router state?
   - In angular router states refers to the current state of the router and the navigation stack within angular application
   - It contains information regarding current active route, parameters, URL, Query params, Navigation History
   - You can access the route state using "Router" serivce.
   - ActivatedRoute allows us to access the current route parameters from the router state.

43) Change detection?
   - It is basically watchful eye that constantly look for changes in your data and updates your 
     application user interface accordingly.
   - When something in you applcation data changes, angular automatically detects those changes and update the 
     corresponding part of UI

44) What is a bootstrapping module?
   - Bootstrapping module is main entrypoint for angular application.
   - It is responsible for staring the aplication and initializing root component
   - it is basically defined in main.ts file.

45) How do you select element from template?
   - You can use various techniques to select component
   - On common approach is to use template reference variables, this can be done using # symbol following up with name
   - Another approach is to used ViewChild & ViewChildren directives to query for elements based on CSS selector & componenttypes

46) How do you deal with errors in observables?
   - catchError operator can be used to handle error and recover from errors
   - This operator allows to execute alternative logic when an errors occurs.

47) what is callbackhell?
   - It is calles as pyramid of doom to describe the situation in asynchronous programming 
     where mutlple nested callback are used to handle asynchronous operations
   - This nesting can be lead to code that is difficult to read, understand, and maintain.
   - Callback avoided using Promises, Async/Await or using rxjs libraries.  

48) Angular Authentication and Authorization.
   - user login credentials are passed to an authenticate API, which is present on the server. 
     Post server-side validation of the credentials, a JWT (JSON Web Token) is returned. 
     The JWT has information or attributes regarding the current user. The user is then identified with the given JWT. 
     This is called authentication.

   - Post logging-in successfully, different users have a different level of access. 
     While some may access everything, access for others might be restricted to only some resources. 
     The level of access is authorization.     

49) How do Observables differ from Promises?
   - As soon as a promise is made, the execution takes place. 
   - However, this is not the case with observables because they are lazy. This means that nothing happens until a subscription is made. 
   - While promises handle a single event, observable is a stream that allows passing of more than one event. 
   - A callback is made for each event in an observable.

50) Directives 
   1) Attribute: ngClass, ngStyle 
      - Attribute directives modify the behavior or appearance of a DOM element.     
   2) Structural: ngIf, ngFor, ngSwitch
      - Structural directives change the structure of the DOM by adding or removing elements based on conditions.
   3) Custom : Highliter, Dropdown

51) What are the building blocks of Angular?
   - Components
   - Data Binding
   - Dependency Injection 
   - Directives
   - Metadata
   - Modules
   - Routing
   - Services
   - Template

52) Components: 
   - A component controls one or more views. Each view is some specific section of the screen
   - Every Angular application has at least one component, known as the root component. 
   - It is bootstrapped inside the main module, known as the root module.
   - A component contains application logic defined inside a class. 
   - This class is responsible for interacting with the view via an API of properties and methods.

53) Dependency Injection (DI) 
    - Angular makes use of DI to provide required dependencies to components.
    - Typically, dependencies required by a component are services.
    - A component s constructor parameters tell Angular about the services that a component requires. 
    - So, a dependency injection offers a way to supply fully-formed dependencies required by a new instance of a class.

54) Directives 
    - The templates used by Angular are dynamic in nature. 
    - Directives are responsible for instructing Angular about how to transform the DOM when rendering a template. 
    - Actually, components are directives with a template. Other types of directives are attribute and structural directives.	


55) Routing 
    - An Angular router is responsible for interpreting a browser URL as an instruction to navigate to a client-generated view. 
    - The router is bound to links on a page to tell Angular to navigate the application view when a user clicks on it.


58) What is Angular Material? 
    - it is a UI component library. Angular Material helps in creating attractive, consistent, and fully functional web pages as well as web applications. 
    - It does so while following modern web design principles, including browser portability and graceful degradation.

59) What is ngOnInit ()? How to define it?
    - ngOnInit () is a lifecycle hook that is called after Angular has finished initializing all data-bound properties of a directive. 
    - It is defined as:

      Interface OnInit {
           ngOnInit () : void
      }

60) What is the process called by which TypeScript code is converted into JavaScript code?
    - It is called Transpiling. Even though TypeScript is used for writing code in Angular applications, 
      it gets internally transpiled into equivalent JavaScript.

61) Why prioritize TypeScript over JavaScript in Angular?
    - TypeScript is developed by Microsoft and it is a superset of JavaScript. 
    - The issue with JS is that it isn t a true OOP language. 
    - As the JS code doesn t follow the Prototype Pattern, the bigger the size of the code the messier it gets. 
    - Hence, it leads to difficulties in maintainability as well as reusability. To avoid this, TypeScript follows a strict OOP approach.

62) What is ViewEncapsulation and how many ways are there do to do it in Angular?
    - To put simply, ViewEncapsulation determines whether the styles defined in a particular component will affect the entire application or not. 
    - Angular supports 3 types of ViewEncapsulation:
      1) Emulated
         - This is the default option.
         - Angular automatically generates unique CSS classes for each component and add them to element 
  	   and prevent them from affecting other component.         
      2) Native   Styles used in other HTML doesn t spread to the component
      3) None   Angular doesnt apply encapsulation. CSS classes defined in component may visible to other component as well
                which will lead to conflicts.

63) What is Data Binding? How many ways it can be done?
    - In order to connect application data with the DOM (Data Object Model), data binding is used. 
    - It happens between the template (HTML) and component (TypeScript). There are 3 ways to achieve data binding:
       1) Event Binding   Enables the application to respond to user input in the target environment
       2) Property Binding   Enables interpolation of values computed from application data into the HTML
       3) Two-way Binding   Changes made in the application state gets automatically reflected in the view and vice-versa. 
          The ngModel directive is used for achieving this type of data binding.

64) What is Transpiling in Angular?
    - Transpiling is the process of converting the typescript into javascript (using Traceur, a JS compiler). 
    - Though typescript is used to write code in the Angular applications, the code is internally transpiled into javascript. 

64) Which of the Angular life cycle component execution happens when a data-bound input value updates?
    - ngOnChanges is the life cycle hook that gets executed whenever a change happens to the data that was bound to an input. 

65) Differentiate between Components and Directives?
    - Components break up the application into smaller parts; whereas, Directives add behavior to an existing DOM element. 

66) Life cycle hooks in angular : 
    - Directive and component instances have a lifecycle as Angular creates, updates, and destroys them. 
    - Developers can tap into key moments in that lifecycle by implementing one or more of the lifecycle hook interfaces in the Angular core library.

    - ngOnChanges - Executes every time the value of input property changes, The hook method recieives a simple changes object containing
		    current and previous property values. This is called before ngOnInit.	

    - ngOnInit - Executes after the constructor and after ngOnChange hook for the first time. 
		 it is commonly used for component initialisation and retriveing data from a database.

    - ngDoCheck - This is for the detection and to act on changes that Angular can't or won't detect on its own.

    - ngAfterContentInit - This is called in response after Angular projects external content into the component's view.

    - ngAfterContentChecked - This is called in response after Angular checks the content projected into the component.

    - ngAfterViewInit - This is called in response after Angular initializes the component's views and child views.

    - ngAfterViewChecked - This is called in response after Angular checks the component's views and child views.

    - ngOnDestroy - This is the cleanup phase just before Angular destroys the directive/component.

72) What are Route Guards? 
    - Angular s route guards are interfaces which can tell the router whether or not it should allow navigation to a requested route.
    - They make this decision by looking for a true or false return value from a class which implements the given guard interface.
    - https://medium.com/@ryanchenkie_40935/angular-authentication-using-route-guards-bf7a4ca13ae3 => JWT Authentication
    - https://codecraft.tv/courses/angular/routing/router-guards/
    - There are five different types of guards and each of them is called in a particular sequence.
      1) CanActivate
	 Checks to see if a user can visit a route.

      2) CanActivateChild
	 Checks to see if a user can visit a routes children.

      3) CanDeactivate
	 - Checks to see if a user can exit a route.
         - Used to warn people if they are navigating away from a page where they have some unsaved changes.

      4) Resolve
 	 - Performs route data retrieval before route activation.
         - ensure whether there is data available or not before navigating to any route. 

      5) CanLoad
	 Checks to see if a user can visit to a module that lazy loaded.

67) Pipes? 
   - Pipe takes in data as input and transforms it to a desired output.
   - This comes from the symbol (|) used to do this operation. 
   - The | character is used to transform data.
   - It takes integers, strings, arrays, and date as input separated with | to be converted in the format as required and display the same in the browser.
   - Type of Pipes: 
     1) Lowercasepipe
     2) Uppercasepipe
     3) Datepipe
     4) Currencypipe
     5) Jsonpipe
     6) Percentpipe
     7) Decimalpipe
     8) Slicepipe
     9) CustomPipe: 
        - To create a custom pipe, we have to import Pipe and Pipe Transform from Angular/core. 
	- In the @Pipe directive, we have to give the name to our pipe, which will be used in our .html file.
	- Before that we need to add the same in app.module.ts in declaration array of NgModule.
	
	.ts file => 
	import {Pipe, PipeTransform} from '@angular/core';
	@Pipe ({
   		 name : 'sqrt'
	       })
	export class SqrtPipe implements PipeTransform {
   		transform(val : number) : number {
      		return Math.sqrt(val);
   		}
	}
	
	Html Declaration: 
	<b>Square root of 25 is: {{25 | sqrt}}</b>

68) HostListener? 
    - @HostListener is Angular's decorator method that's used for listening to DOM events on the host element of both component and attribute directives. 
    - @HostListener sets the listeners once the directive is initialized and removes them automatically once the directive gets destroyed.
    - @HostListener catches and listens to events from children or nested elements when they bubble up to the host element. 
      Moreover, you can listen to events on global elements, but you should do so sparingly for performance reasons.

69) which class used to make http calls in angular?
    - HttpClient => import { HttpClientModule } from '@angular/common/http';

70) How to share data between angular components?
   - Parent to Child: via Input
   - Child to Parent: via Output() and EventEmitter
   - Child to Parent: via ViewChild
   - Unrelated Components: via a Service
   - https://www.dotnettricks.com/learn/angular/sharing-data-between-angular-components-methods
   - https://angularfirebase.com/lessons/sharing-data-between-angular-components-four-methods/

71) What is viewchild? 
    - ViewChild allows a one component to be injected into another, giving the parent access to its attributes and functions. 
    - @ViewChild allows the parent component to access the child component's properties and methods.



73) Navigate from one view to another on button click?
    Approach 1 : 
	- You have to inject Router in the constructor like this: constructor(private router: Router) { }
        - this.router.navigateByUrl('/user');
	
    Approach 2: 
 	- After Angular v4 you can directly add routerLink attribute on the button
 	- <button routerLink="/url"> Button Label</button>

74) What is Route Outlet?  
    - The RouterOutlet is a directive from the router library that is used like a component. 
    - It acts as a placeholder where router will render the component associated with current route
    - Acts as a placeholder that Angular dynamically fills based on the current router state.

75) Difference between var, let and const?
   - var declarations are globally scoped or function scoped while let and const are block scoped.
   - var variables can be updated and re-declared within its scope; let variables can be updated but not re-declared; const variables can neither be updated nor re-declared.
   - They are all hoisted to the top of their scope but while var variables are initialized with undefined, let and const variables are not initialized.
   - While var and let can be declared without being initialized, const must be initialized during declaration.

76) What Are Angular Reactive Forms?
    - Reactive forms are also known as model-driven forms. 
    - They employ a technique where the forms are designed in the component and then bindings are done for the HTML.
    - In reactive we basically use FormGroup, formBuilder, FormControl classes.

77) What Are Angular Template-Driven Forms?
    - Template-driven forms are driven by Directives in the template. 
    - This means that you will see Directives such as ngModel in the template as opposed to the code. 

78) Differences Between Template-Driven and Reactive Forms?
    - Template-driven forms use the FormsModule, while reactive forms use the ReactiveFormsModule.
    - Template-driven forms are asynchronous, while reactive forms are synchronous.
    - In template-driven forms, most of the interaction occurs in the template, 
      while in reactive-driven forms, most of the interaction occurs in the component.
    - Although template forms are easier to create, they become a challenge when you want to do unit testing, 
      because testing requires the presence of a DOM. Also they become challenge when your form is more complex.
    - It's easier to write unit tests in reactive forms since all the form code and functionality is contained in the component. 
      However, reactive forms require more coding implementation in the component.

79) What's the difference between an Angular component and module?
    - Components control views (html). They also communicate with other components and services to bring functionality to your app.
    - Modules consist of one or more components. They do not control any html. 
      Your modules declare which components can be used by components belonging to other modules, 
      which classes will be injected by the dependency injector and which component gets bootstrapped. 
      Modules allow you to manage your components to bring modularity to your app.

80) How can I select an element in a component template?
    You can get a handle to the DOM element via ElementRef by injecting it into your component's constructor:

81) Ahead-of-Time (AOT) compiler?
    - An Angular application consists mainly of components and their HTML templates. 
    - Because the components and templates provided by Angular cannot be understood by the browser directly, Angular applications require a compilation process before they can run in a browser.
    - The Angular Ahead-of-Time (AOT) compiler converts your Angular HTML and TypeScript code into efficient JavaScript code during the 
      build phase before the browser downloads and runs that code. 
    - Compiling your application during the build process provides a faster rendering in the browser.
    - Angular offers two ways to compile your application:
      1. Just-in-Time (JIT), which compiles your app in the browser at runtime.
      2. Ahead-of-Time (AOT), which compiles your app at build time.
    - With AOT, the browser downloads a pre-compiled version of the application. 
      The browser loads executable code so it can render the application immediately, without waiting to compile the app first.

82) What are the utility functions provided by RxJS?
    The RxJS library also provides below utility functions for creating and working with observables.
    - Converting existing code for async operations into observables
    - Iterating through the values in a stream
    - Mapping values to different types
    - Filtering streams
    - Composing multiple streams

83) How to set headers for every request in Angular?
    - Create service with Injectable() decorator.
    - Import Http & Headers from '@angular/http'
    - Create let headers = new Headers(); object.
    - append headers and add into http.get or http.post.

84) FromArray?
    - FormArray is a variant of FormGroup. 
    - The key difference is that its data gets serialized as an array (as opposed to being serialized as an object in case of FormGroup). 
    - This might be especially useful when you dont know how many controls will be present within the group, like dynamic forms.

85) HttpInterceptor? 
    - HttpInterceptor was introduced with Angular 4.3. It provides a way to intercept HTTP requests and responses 
      to transform or handle them before passing them along.
    - You can use them to set headers (JWT for example), redirect on error responses, and generally will help you avoid code duplication.
    - URL => want to change HTTP to HTTPS.
    - Loader => Everyone wants to see the spinning wheel of fortune when we are waiting for a response. 
      What if I said we could set it up centrally in an interceptor so that we show a loader whenever there are active requests.

86) What is CLI?
    - Its command line interface.
    - As the name implies, it is a command line tool for creating angular apps.
    - It is recommended to use angular cli for creating angular apps as you don't need to spend time installing 
      and configuring all the required dependencies and wiring everything together.

87) What is Webpack?
    - Its powerful module bundler,a tool for bundling application source code in convenient chunks and for loading that code from a server into a browser.
    - it can bundle your js,css,html and many other development things and module is nothing but a chunk of code written in your files.

88) Angular app Deployment?
    Step 1: Enable Production Mode 
           - Angular apps run in development mode by default.
           - To enable production mode when running remotely, edit the code in src/main.ts. 
           - // Enable production mode unless running locally
             if (!/localhost/.test(document.location.host)) { enableProdMode(); }

    Step 2: Edit Index.cshtml to replace some of js files as well as add new js files references in the page.

    Step 3: Build your App
            - Next, run the build command on your project using ng build
            - After successfully building, it will create a dist folder in your directory which contains all necessary files built from your project.

    Step 4: Upload your App
            - You can now upload all the files in the /dist folder to your server.
            - After you have uploaded the app, please note that if you uploaded the files in a folder that is not relative to the domain, 
              you will need to edit the <base> tag in your index.html.

99) Observable?
    - Angular uses the Observer pattern which simply means   Observable objects are registered, and other objects observe 
      (in Angular using the subscribe method) them and take action when the observable object is acted on in some way. They are similar to promises

100) Lifecycle of sevices? 
    - Service can have 2 scopes.
    - if service is declared on your module, you have same instance shared for all, 
      this means service will be constructed when the first component/directive/service/Pipe who needs it will be created. 
      Then it will be destroyed when Module itself will be destroyed (most of the time when page is unloaded)
    - if the service is declared on Component/Directive/Pipe, then 1 instance will be created each time 
      when Component/Directive/Pipe will be created and destroyed when related Component/Directive/Pipe will be destroyed.

101) Dependencey Injection?
    - its basically pattern where class recieves its dependencies from external source rather creating them itself.
    - Here external source is Angular Injector who is responsible for creating instance of these dependenceies.
    - Easy to write and mainitain unit tests as the dependencies can be mocked.
    - Easy to share the data and functionality as the angular injector provides singletone i.e. single instance of service.

102) Could you explain services in Angular?
    - Singleton objects in Angular that get instantiated only once during the lifetime of an application are called services.
    - An Angular service contains methods that maintain the data throughout the life of an application. 
    - The primary intent of an Angular service is to organize as well as share business logic, models, 
      or data and functions with various components of an Angular application.
    - The functions offered by an Angular service can be invoked from any Angular component, such as a controller or directive.

103) What is the main difference between constructor and ngOnInit?
    - The constructor is a feature of the class itself, not Angular.
    - The main difference is that Angular will launch ngOnInit after it has finished configuring the component.
    - Meaning, it is a signal through which the @Input() and other banding properties and decorated properties are available in ngOnInit, 
      but are not defined within the constructor by design.

104) What modules should you import in Angular to use [(ngModel)] and reactive forms?
    - FormsModule and Reactiveforms Module.

105) How similar is AngularJS to Angular 2?
    - Both are front-end frameworks maintained by Google, but Angular 2 is not a simple update of AngularJS, 
      it is a new framework written from scratch. Updating an app from AngularJS to Angular 2 would require a complete rewrite of the code.

106) What were some features introduced in the different versions of Angular (2, 4, 5 and 6)?
    - Angular 2
      - Complete rewrite of the Angular framework
      - Component-based rather than controllers/view/$scope. This allows more code to be reused, easier communication between components and easier testing

      - Much faster
      - Support for mobile devices
      - More language choices such as TypeScript
   
    - Angular 4
      - An update to Angular 2, not a complete rewrite. Updating from Angular 2 to 4 just requires updating the core libraries
      - Improvements to Ahead-of-time (AOT) generated code
      - Support for new versions of TypeScript
      - Animation packages are removed from the core package
      - Else block
    
    - Angular 5
      - Focused on making Angular smaller and faster to use
      - Http is depreciated and HttpClient API client is now recommended for all apps
      - Supports TypeScript 2.3
      - Introduction of a build optimizer
      - Angular Universal State Transfer API
      - Improvements to the Angular Compiler
      - Router Lifecycle Events
      - Better cross-browser standardization
  
     - Angular 6
        - Better service worker support
	- Better URL serialization
	- Ivy rendering engine
	- ng update and ng add
	- <template> element completely removed
	- Angular Elements/Custom Elements
	- Form validation changes
	- Schematics
    

107) Why do we need lazy loading of modules and how is it implemented?
    - Lazy loading of modules is needed to break the code into pieces
    - When downloading the app in the browser, it doesn t load all of the application code
    - During the transition to the route with lazy loading, the module has to load the code into a browser.
    - Exemple for using lazy loading modules:
      { path:  example , loadChildren:  ./example/example.module#ExampleModule , component:PublicComponent }

108) what is an rxjs subject in Angular
    - An RxJS Subject is a special type of Observable that allows values to be multicasted to many Observers.
    - While plain Observables are unicast (each subscribed Observer owns an independent execution of the Observable), Subjects are multicast.
    - A Subject is like an Observable, but can multicast to many Observers. 
    - Subjects are like EventEmitters: they maintain a registry of many listeners.

109) What happens if I import the same module twice?
    - If multiple modules imports the same module then angular evaluates it only once (When it encounters the module first time).
    - It follows this condition even the module appears at any level in a hierarchy of imported NgModules.

110) Promise?
    - Promises are used to handle asynchronous operations in JavaScript.
    - They are easy to manage when dealing with multiple asynchronous operations 
      where callbacks can create callback hell leading to unmanageable code and
      provide better error handling than callbacks and events.
    - we may say that, promises are the ideal choice for handling multiple callbacks at the same time, 
      thus avoiding the undesired callback hell situation.

111) States of promise?
    - fulfilled: Action related to the promise succeeded
    - rejected: Action related to the promise failed
    - pending: Promise is still pending i.e. not fulfilled or rejected yet
    - settled: Promise has fulfilled or rejected

112) Promise Consumers
    - then() => then() is invoked when a promise is either resolved or rejected. 
      It may also be defined as a career which takes data from promise and further executes it successfully.
    - then() method takes two functions as parameters.  
      1) First function is executed if promise is resolved and a result is received.
      2) Second function is executed if promise is rejected and an error is received.
    - catch() => catch() is invoked when a promise is either rejected or some error has occurred in execution. 

113) Bootstraping?
    - The process of loading the index.html page, app-level module, and app-level component is called bootstrapping, or loading the app.
    - Angular takes the following steps to bootstrap the application:
      1) Load index.html
      2) Load Angular, Other Libraries, and App Code
      3) Execute main.ts File
      4) Load App-Level Module
      5) Load App-Level Component
      6) Process Template
    
# MEAN Stack Interview Questions

---

## JavaScript

1. **Call, Apply, Bind** in JavaScript
2. How to **add a custom method** in the default `Array` class
3. **Closures**
4. If I have 5 API calls, and I want to **prioritize the last API** in case the 2nd one fails — how can we achieve this?
5. How **Observables** work behind the scene — where is the stream of data stored?
6. How to make an **object immutable**

---

## Angular

1. How to **pass parameters** in Angular pipes?
2. What are **data passing strategies** in Angular?
3. What are **hooks** in SPA frameworks?
4. What is the difference between **prod build** and **local build**?
5. **RxJS operators** — `map`, `of`, `from`, `mergeMap`, `concatMap`, `switchMap`, `exhaustMap`
6. Difference between **Subject** vs **BehaviourSubject**
7. **Directives** in Angular — can we use multiple directives together?
8. **Lazy loading** in Angular
9. **Standalone component** in Angular 10+
10. `ngSwitch`, `trackBy`
11. `ViewChild`, `ViewContainerRef` and `TemplateRef`
12. Difference between `ng-content` and `ng-template`
13. Difference between **React.js** and **Angular**
14. **Signals** in Angular, **NgRx**, state management in Angular
15. **Specificity** in HTML/CSS
16. What is `NgModule` in Angular
17. What is **bootstrap** in Angular
18. **HTTP Interceptors** in Angular

---

## Node.js

1. How **microservices** are managed in Node.js?
2. **Preflight request**
3. What is **handshaking** in real-time communication?
4. How to fetch **1 GB of data** from server and store it again
5. Difference between **authentication** and **authorization**
6. Difference between **Parallelism** vs **Concurrency**
7. **Event loop** in Node.js
8. Difference between `PUT`, `POST`, `GET`, `PATCH` requests
9. **Server-Side Rendering (SSR)**
10. What is **callback hell** and how can we tackle this problem?
11. **Event Emitter** in Node.js

---

## MongoDB

1. What is **Indexing**, **Aggregation**, **Projection**?
2. How to access **multiple collections** in MongoDB?
3. **Sharding**
4. `$var` vs `$expression`

---

## SQL

1. What will happen if we put `GROUP BY` **before** sort and **after** sort?
2. How to prevent invalid access to data in **raw SQL queries** — how to validate raw SQL queries?
3. **SQL Injection**
4. Get **2nd highest salary**, `SUM()`, `GROUP BY`
5. How to **JOIN** two tables

---

## Real-Time Communication

1. **Pusher.io** — how does it establish connection between server and frontend?
2. How does **Pusher** work behind the scenes?

---

## DevOps

1. **AWS Lambda**
2. **S3**
3. Limitations of using **AWS Lambda**
4. **Caching strategies**
5. **Nginx**
6. **Jenkins**
7. **Docker**
8. **Kubernetes**
9. **Kafka**
10. **OAuth**
11. How to **secure a microservice**

---

## Data Structures

1. Find index of a target using **binary search**
